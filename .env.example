# Partagé par Docker Compose et Django
DJANGO_SETTINGS_MODULE=config.settings.production
SECRET_KEY=<générer: python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())">

# PostgreSQL
POSTGRES_DB=cuisinesync
POSTGRES_USER=cuisinesync
POSTGRES_PASSWORD=changeme
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Django production
ALLOWED_HOSTS=212.227.88.212,votre-domaine.fr
CORS_ALLOWED_ORIGINS=https://votre-domaine.fr
CSRF_TRUSTED_ORIGINS=https://votre-domaine.fr
